---
- name: Deploy olftransfer
  hosts: vps
  become: true

  vars:
    app_dir: /opt/olftransfer
    image: ghcr.io/{{ lookup('env', 'GITHUB_REPOSITORY') | default('Bxota') }}
    image_tag: "{{ image_tag | default('latest') }}"

  roles:
    - nginx
    - postgres
    - app
